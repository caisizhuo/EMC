spring:
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/emc?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  jpa:
    database: MYSQL
    show-sql: true
    open-in-view: true
    hibernate:
      ddl-auto: update
  mvc:
    date-format: yyyy-MM-dd